[project]
name = "servicekit-demo-auth"
version = "0.1.0"
description = "API key authentication demo"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "servicekit",
]

[tool.uv.sources]
servicekit = { path = "../..", editable = true }
