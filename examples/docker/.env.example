# Servicekit API Keys (comma-separated)
# Generate secure keys with: openssl rand -base64 32
SERVICEKIT_API_KEYS=sk_dev_example123,sk_dev_example456

# Logging configuration
LOG_FORMAT=json
LOG_LEVEL=INFO

# Example module to run
EXAMPLE_MODULE=examples.auth_envvar:app
